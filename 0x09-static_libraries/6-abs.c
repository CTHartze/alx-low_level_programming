#include "main.h"

/**
 * _abs - Calculates absolute value of an integer.
 * @n: refers to int to check
 * Return: absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
